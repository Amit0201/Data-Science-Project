#Step 1 ---> Loading the Dataset and basic Data Exploration

import pandas as pd
import numpy as np
import seaborn as sns
import matplotlib.pyplot as plt

df = pd.read_csv('train.csv')
print("Dataset loaded successfully.")
print(f"-----------------------------------------------------------------------------------------------------------------------------------------------------------")
print(df.head())
print(f"-----------------------------------------------------------------------------------------------------------------------------------------------------------")
rows, columns = df.shape
print(f"Rows, Columns = {rows, columns}")
print(f"-----------------------------------------------------------------------------------------------------------------------------------------------------------")
print(f"Name of columns = {df.columns}")
print(f"-----------------------------------------------------------------------------------------------------------------------------------------------------------")
print(df.info())